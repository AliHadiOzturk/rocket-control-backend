const connectionTypes = {
    ROCKET: 'rocket'
}

const dataTypes = {
    STRING: 'string',
    FLOAT: 'float',
    INTEGER: 'integer',
    BOOLEAN: 'boolean',
    SHORT: 'short',
    BYTE: 'byte'
}

module.exports = { connectionTypes, dataTypes }